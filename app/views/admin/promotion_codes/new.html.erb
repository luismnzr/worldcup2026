<% content_for(:page_title) { "Nuevo Código de Descuento" } %>
<div class="max-w-2xl">
  <%= link_to "&larr; Volver".html_safe, admin_promotion_codes_path, class: "text-sm text-[var(--color-muted-foreground)] hover:text-[var(--color-foreground)] mb-4 inline-block" %>
  <div class="bg-white rounded-xl border border-[var(--color-border)] p-6">
    <h2 class="text-lg font-semibold mb-6">Nuevo Código de Descuento</h2>
    <%= render "form", promotion_code: @promotion_code %>
  </div>
</div>
